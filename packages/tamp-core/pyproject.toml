[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tamp-core"
version = "0.8.0"
description = "Shared foundation for the tamp toolkit."
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/tamp_core"]